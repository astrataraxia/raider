# Core

- Workspace root: `C:\Users\astra\src\raider`.
- Goals 1 through 5 are complete. Current work is Goal 6 searchable live tile web UI.
- The repository contains a .NET 10 solution, planning contracts, API spike scripts and fixtures, and a static design prototype.
- This workspace is a Git repository without an initial commit yet.
- Read `mem:tech_stack` for chosen stack once initialized, `mem:suggested_commands` for runnable commands, `mem:conventions` for project-specific style, and `mem:task_completion` for done checks.
