# Core

- Workspace root: `C:\Users\astra\src\raider`.
- Raider `v1.0.0` is complete. There is no active Goal.
- The repository contains a .NET 10 solution, product and engineering contracts, deployment guidance, API spike scripts, and fixtures.
- Completed implementation history and verification evidence are summarized in `RELEASES.md`.
- Read `mem:tech_stack` for chosen stack once initialized, `mem:suggested_commands` for runnable commands, `mem:conventions` for project-specific style, and `mem:task_completion` for done checks.
