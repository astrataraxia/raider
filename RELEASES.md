# Raider 릴리스 기록.

## v2.0.1. 2026-09-06.

- **플랫폼 키 누락 격리**: CHZZK 또는 SOOP 인증 설정이 없어도 프로세스가 시작된다. 키가 없는 플랫폼만 Configuration 오류로 화면에 표시하고, 키가 있는 플랫폼 수집과 HTTP 응답은 계속한다.
- **화면 안내**: 한쪽만 빠지면 `CHZZK API 키가 설정되지 않았습니다` 같은 배너와 나머지 플랫폼 타일을 함께 보여준다. 둘 다 없으면 재시도 문구 대신 키 미설정을 표시한다.
- **수집 계약**: 클라이언트 생성자에서 키를 검사해 기동을 중단하지 않는다. 수집 시점에 키가 없으면 HTTP 호출 없이 Configuration 오류를 게시한다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 자동 테스트 | Playwright 제외 `dotnet test` 93개 통과. 기동, 수집 Configuration 오류, 홈 화면 배너 계약을 포함. |
| 코드 포맷 | `dotnet format --verify-no-changes` 통과. |
| 빌드 | `dotnet build --no-restore -warnaserror`에서 C# 경고 0개. 이 환경의 NuGet 감사 캐시 권한으로 `NU1900`이 발생할 수 있다. |
| Playwright | 이 환경에 Chromium이 없어 브라우저 테스트는 실행하지 않았다. |

## v2.0.0. 2026-08-30.

- **SOOP 공식 API 전환**: 기존 공개 웹 JSON 수집을 SOOP Developers 공식 `broad/list` API로 교체했다. 운영 설정의 `Raider:Soop:ClientId` 또는 `RAIDER__SOOP__CLIENTID`가 필요하다.
- **SOOP 카테고리 API 연동**: 공식 `broad/category/list` 응답을 재귀적으로 읽어 카테고리 이름을 공통 태그로 제공한다. 카테고리 메타데이터가 일시적으로 실패해도 방송 수집 자체는 계속한다.
- **시청자 수 계약 결정**: 공식 응답의 `total_view_cnt`를 공통 모델의 현재 시청자 수로 매핑한다.
- **즐겨찾기 호환성 유지**: 기존과 동일하게 SOOP `broad_no`를 `channel_id`로 사용한다. SQLite 즐겨찾기 파일 경로와 테이블 구조는 변경하지 않으며, 구버전 파일에 `category`가 없을 때만 기존 행을 보존한 채 컬럼을 추가한다.
- **배포 버전 상향**: 공식 API 인증과 응답 계약 전환을 포함하는 큰 내부 호환성 변경으로 프로젝트 버전을 `2.0.0`으로 올렸다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 실제 일반 runtime 기동 | 시스템 `Microsoft.AspNetCore.App 10.0.11`에서 SOOP 공식 API 수집 성공, 1,391개 방송 확인. |
| 실제 앱 HTTP | `/health/live` 200, SOOP 홈 200, 재생 링크 120개와 카테고리 태그 링크 렌더링 확인. |
| 실제 필터 | SOOP 카테고리 필터 요청 200 및 결과 화면 렌더링 확인. |
| Chzzk 격리 | 검증 프로세스에서 Chzzk 수집을 비활성화하고 SOOP 요청만 확인. |
| 자동 테스트 | `dotnet test` 88개 통과. |
| 코드 포맷 | `dotnet format --verify-no-changes --no-restore` 통과. |
| 빌드 | `dotnet build --no-restore -warnaserror` 경고 0개, 오류 0개. |

## v1.3.11. 2026-07-12.

- **SOOP 일시 네트워크 장애 복구**: 나머지 페이지를 순차 수집하고, DNS 또는 TCP의 일시 오류가 난 페이지는 최대 3회까지 재시도한다. 한 페이지의 순간 실패로 전체 SOOP 목록을 폐기할 가능성을 낮춘다.
- **운영 원인 확인**: Docker resolver의 외부 DNS 질의 timeout이 SOOP 및 CHZZK의 `Network` 실패와 같은 시각에 반복됨을 확인했다.

## v1.3.10. 2026-07-11.

- **수집 실패 원인 로그**: 수집 재시도와 최종 실패에 내부 예외를 함께 기록해 DNS, TLS, 연결, HTTP 오류를 운영 컨테이너 로그에서 구분할 수 있게 한다.

## v1.3.9. 2026-07-11.

- **운영 수집 timeout 일원화**: CHZZK 5초, SOOP 15초의 HTTP 클라이언트 제한을 제거하고 기존 플랫폼별 전체 수집 timeout 180초만 적용한다. 느린 외부 API를 조기 수집 실패로 오인하지 않는다.
- **회귀 테스트**: 두 플랫폼 HTTP 클라이언트가 별도 timeout 없이 수집 worker의 취소 토큰을 따르는지 고정했다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 공개 사이트 재현 | 수동 새로고침에서 CHZZK `Timeout` 11.1초, SOOP `Network` 17.9초를 브라우저 Console과 운영 컨테이너 로그에서 확인. |
| 전체 자동 테스트 | `dotnet test tests\Raider.Web.Tests\Raider.Web.Tests.csproj --no-restore` 86개 통과. |
| 코드 포맷 | `dotnet format tests\Raider.Web.Tests\Raider.Web.Tests.csproj --verify-no-changes --no-restore` 통과. |
| 빌드 | `dotnet build src\Raider.Web\Raider.Web.csproj --no-restore -warnaserror` 경고 0개, 오류 0개. |

## v1.3.8. 2026-07-11.

- **수집 결과 진단**: 플랫폼별 마지막 수집 성공 또는 실패, 전체 소요 시간, 안전한 오류 종류를 상태 API로 제공한다.
- **브라우저 Console 출력**: 수동 새로고침이 완료되면 CHZZK과 SOOP의 수집 결과를 각각 한 줄로 출력해 지연 또는 실패한 API를 즉시 구분할 수 있다.
- **실제 Compose 검증**: 운영용 CHZZK 인증정보를 주입한 Docker Compose에서 초기 수집과 수동 새로고침 수집이 두 플랫폼 모두 성공함을 확인했다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 전체 자동 테스트 | `dotnet test tests\Raider.Web.Tests\Raider.Web.Tests.csproj --no-restore` 85개 통과. |
| 초기 컨테이너 수집 | CHZZK 3,856개 약 3.99초, SOOP 3,118개 약 1.31초에 성공. |
| 수동 새로고침 수집 | CHZZK 3,870개 약 5.35초, SOOP 3,137개 약 0.90초에 성공. |

## v1.3.7. 2026-07-03.

- **SOOP 운영 timeout 확대**: 운영 서버에서 SOOP 공개 웹 JSON 페이지 일부가 로컬보다 느리게 응답해 전체 수집이 반복 timeout되는 현상을 확인하고, SOOP 요청 timeout을 15초, 전체 수집 timeout을 180초로 늘렸다.
- **SOOP 설정 회귀 테스트**: SOOP 전체 수집 timeout도 대형 페이지네이션과 느린 운영망을 감당하는 값인지 설정 테스트로 고정했다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 설정 테스트 | `dotnet test tests\Raider.Web.Tests\Raider.Web.Tests.csproj --filter CollectionOptionsTests --no-restore` 2개 통과. |

## v1.3.6. 2026-07-03.

- **CHZZK 수집 timeout 확대**: 운영에서 CHZZK 전체 라이브 규모가 6천 개 이상으로 커지며 20개 단위 순차 페이지 수집이 60초 제한에 걸릴 수 있어, CHZZK 전체 수집 timeout을 180초로 늘렸다.
- **운영 기본값 회귀 테스트**: CHZZK 수집 timeout이 대형 페이지네이션 규모를 감당하는지 설정 테스트로 고정했다.
- **SOOP 병렬 테스트 안정화**: 병렬 수집 테스트의 최대 동시성 관측값을 원자적으로 갱신해 테스트 race를 제거했다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 설정 테스트 | `dotnet test tests\Raider.Web.Tests\Raider.Web.Tests.csproj --filter CollectionOptionsTests --no-restore` 1개 통과. |
| SOOP 병렬 테스트 반복 | `dotnet test tests\Raider.Web.Tests\Raider.Web.Tests.csproj --filter FullyQualifiedName~SoopClientTests.PublishesFirstPageAndFetchesRemainingPagesConcurrently --no-restore` 10회 통과. |
| 전체 자동 테스트 | `dotnet test tests\Raider.Web.Tests\Raider.Web.Tests.csproj --no-restore` 83개 통과. |
| 코드 포맷 | `dotnet format --verify-no-changes` 통과. |

## v1.3.5. 2026-06-28.

- **부분 새로고침 전환**: 수집 완료 감지 시 `window.location.reload()`로 전체 문서를 다시 불러오던 흐름을 제거하고, 현재 HTML을 fetch한 뒤 헤더 상태, 통계, 라이브 결과 영역만 교체하도록 바꿨다.
- **새로고침 버튼 UX 개선**: 앱 내부 새로고침 버튼 제출을 fetch 기반으로 처리해 URL과 사이드바 상태를 유지하면서 수집 진행 상태와 완료 결과를 반영한다.
- **즐겨찾기 재바인딩 안정화**: 라이브 카드 영역이 부분 교체된 뒤에도 즐겨찾기 버튼 상태와 클릭 핸들러가 다시 적용되도록 `favorites.js`에 공개 refresh hook과 중복 방지 바인딩을 추가했다.
- **배포 태그 정리**: 패키지 버전, README, 운영 문서, favicon 캐시 파라미터를 `v1.3.5` 기준으로 갱신했다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 전체 자동 테스트 | `dotnet test --no-restore` 82개 통과. |
| 빌드 | `dotnet build src\Raider.Web\Raider.Web.csproj --no-restore` 경고 0개, 오류 0개. |
| 실제 브라우저 | 로컬 `http://127.0.0.1:5127/`에서 새로고침 버튼 클릭 후 URL 유지, `data-live-content` 부분 교체, 새로고침 버튼 재등장, 즐겨찾기 버튼 120개 재바인딩 확인. |
| 배포 사이트 관찰 | `https://raider.astral-ataraxia.com/` 첫 로드 대기가 45초 브라우저 제어 타임아웃에 걸릴 정도로 길어 기존 전체 reload UX 문제가 재현됐다. |

## v1.3.4. 2026-06-27.

- **한국 시간 고정 표시**: 홈 화면의 스냅샷 갱신 시각을 서버 또는 컨테이너 로컬 타임존과 무관하게 `Asia/Seoul` 기준 `HH:mm`으로 표시한다.
- **SQLite 전이 취약 패키지 해소**: `SQLitePCLRaw.bundle_e_sqlite3`를 3.0.3으로 명시해 `SQLitePCLRaw.lib.e_sqlite3` 2.1.11의 `NU1903` 취약점 경고를 제거했다.
- **문서 최신화**: README, 제품 계획, 엔지니어링 계약, 디자인 계약, 배포 문서를 `v1.3.4`와 현재 시간 표시 계약 기준으로 정리했다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 홈 화면 테스트 | `dotnet test --filter "FullyQualifiedName~HomePageTests"` 10개 통과. |
| 전체 자동 테스트 | `dotnet test` 82개 통과. |
| 취약 패키지 확인 | `dotnet list src/Raider.Web/Raider.Web.csproj package --include-transitive --vulnerable` 취약 패키지 없음. |
| 코드 포맷 | `dotnet format --verify-no-changes` 통과. |
| 빌드 및 경고 | `dotnet build --no-restore -warnaserror` 경고 0개, 오류 0개. |
| 공백 검사 | `git diff --check` 통과. |

## v1.3.3. 2026-06-16.

- **빠른 수동 새로고침**: 새로고침 요청이 수집 worker를 즉시 실행하고, CHZZK과 SOOP 모두 첫 페이지 결과를 부분 스냅샷으로 먼저 게시해 인기 방송이 더 빨리 화면에 반영된다.
- **SOOP 수집 병렬화**: SOOP 나머지 페이지를 최대 4개 요청으로 제한해 병렬 수집하고, 페이지 DTO를 오래 보관하지 않고 즉시 공통 `LiveStream`으로 변환한다.
- **부분 갱신 안전성**: 이후 페이지 실패 시 부분 결과를 최종 성공 결과로 확정하지 않고 마지막 완성 스냅샷으로 복구한다.
- **메모리와 로그 정리**: `LiveStream`에서 중복 검색 문자열과 `Uri` 객체 보관을 줄이고, 반복 HTTP client 정보 로그를 억제했다.
- **죽은 CSS 정리와 즐겨찾기 보존**: 사용하지 않는 CSS를 제거하고, 즐겨찾기 반복 등록 시 기존 카테고리를 유지한다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 자동 테스트 | 이전 검증에서 82개 통과. 현재 Windows 로컬에서는 애플리케이션 제어 정책이 테스트 출력 `Raider.Web.dll` 로드를 차단해 재실행 불가. |
| 코드 포맷 | `dotnet format --verify-no-changes` 통과. |
| 빌드 및 경고 | `dotnet build --no-restore -warnaserror` 경고 0개, 오류 0개. |
| 공백 검사 | `git diff --check` 통과. |

## v1.3.2. 2026-06-14.

- **즐겨찾기 카테고리 필터**: 메인 화면에서 `[즐겨찾기]` 필터가 켜졌을 때, 즐겨찾기에 속한 고유 카테고리들이 서브 필터 바로 노출되며, 특정 카테고리를 눌러 필터링할 수 있는 기능을 제공한다. 카테고리 필터 선택 상태는 플랫폼, 검색, 페이지네이션과 완벽히 동기화된다.
- **Favicon 캐싱 및 라우팅 해결**: 브라우저 탭에 Favicon이 뜨지 않던 문제를 캐시 방지 파라미터(`?v=1.3.2`) 및 단일 `/favicon.ico` redirect 처리로 완전히 해결하여, 보라색 concentric circle 디자인의 브랜드 마크가 브라우저 탭에 항상 안정적으로 노출되게 했다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 자동 테스트 | 로컬에서 73개 테스트 통과 (카테고리 필터링 통합 테스트 추가). |
| 코드 포맷 | `dotnet format --verify-no-changes` 통과. |
| 빌드 및 경고 | `dotnet build --no-restore -warnaserror` 경고 0개, 오류 0개. |
| 실제 브라우저 | 카테고리 필터 선택 시 해당 스트리머만 렌더링 검증, 액티브 카테고리 칩 정상 노출 및 필터 해제 확인, 보라색 동그라미 브랜드 Favicon 탭 정상 렌더링 확인. |

## v1.3.1. 2026-06-14.

- **카테고리 접고 펼치기**: 사이드바 내의 카테고리 헤더를 클릭하여 접거나 펼칠 수 있으며, chevron 아이콘(`▼`/`▶`)으로 접힘 상태를 표시하고 이 상태는 `localStorage`를 통해 영구 보존된다.
- **드래그 앤 드롭 카테고리 이동**: 스트리머 행을 원하는 카테고리로 직접 드래그 앤 드롭하여 소속을 편리하게 옮길 수 있으며, 기존의 인라인 드롭다운을 제거해 UI를 단순화했다.
- **카테고리 순서 재배치**: 카테고리 헤더 자체를 드래그하여 위아래로 순서를 직관적으로 변경할 수 있으며, 이 순서 역시 로컬 브라우저에 저장되어 영구 보존된다.
- **시청자 수 내림차순 정렬**: 각 카테고리 내부의 즐겨찾기 스트리머들을 라이브 시청자 수 내림차순(오프라인은 이름순)으로 자동 정렬했다.
- **새 카테고리 ➕ 추가**: 즐겨찾기 헤더의 ➕ 버튼으로 빈 카테고리를 직관적으로 미리 생성하여 스트리머를 드래그 앤 드롭할 대상을 쉽게 준비할 수 있다.
- **Favicon 탭 렌더링 수정**: 루트 `<svg>`의 width/height 속성을 추가하고, `/favicon.ico` 주소 직접 호출 시에도 올바른 MIME type의 svg 파일로 응답하도록 미들웨어에 라우팅을 추가하여 favicon이 탭에 나타나지 않던 문제를 해결했다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 자동 테스트 | 로컬에서 72개 테스트 통과. |
| 코드 포맷 | `dotnet format --verify-no-changes` 통과. |
| 빌드 및 경고 | `dotnet build --no-restore -warnaserror` 경고 0개, 오류 0개. |
| 실제 브라우저 | 카테고리 접기/펴기 로컬스토리지 보존, 카테고리 및 스트리머 드래그 앤 드롭 이동 확인, 빈 카테고리 생성 확인, 시청자 순 정렬 확인. |

## v1.3.0. 2026-06-14.

- **즐겨찾기 카테고리 관리**: 즐겨찾는 스트리머를 카테고리별로 분류하여 폴더(📁) 형태로 관리하고, 인라인 드롭다운으로 즉시 다른 카테고리로 변경하거나 새 카테고리를 추가할 수 있는 기능을 추가했다.
- **정보 밀도 개선 및 시청인원 표시**: 사이드바의 즐겨찾기 항목 간격(높이 및 여백)을 좁히고, 라이브 중인 스트리머 우측에 현재 시청자 수를 표시했다.
- **즐겨찾기 전용 검색 필터**: 상단 플랫폼 필터 바에 `[즐겨찾기]` 버튼과 필터 칩을 추가하여 현재 즐겨찾기 등록된 채널들만 필터링하여 볼 수 있도록 하고, 검색어/플랫폼/태그와의 결합 검색을 지원했다.
- **브라우저 Favicon 적용**: Raider 전용 브랜드 로고가 브라우저 탭에 표시되도록 커스텀 SVG Favicon을 등록했다.
- **안전한 스키마 마이그레이션**: SQLite 테이블에 `category` 컬럼을 신설하고 데이터베이스 시작 시 자동 ALTER TABLE 마이그레이션을 지원했다.
- **Playwright E2E 테스트 보강**: 반응형 뷰포트 상태 검사에서의 Flake 현상을 `page.WaitForFunctionAsync`를 도입하여 해결하고 전체 CI 안정성을 다졌다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 자동 테스트 | 로컬에서 72개 테스트 통과. |
| 코드 포맷 | `dotnet format --verify-no-changes` 통과. |
| 빌드 및 경고 | `dotnet build --no-restore -warnaserror` 경고 0개, 오류 0개. |
| 실제 브라우저 | 카테고리 추가 및 인라인 이동, 시청인원 배지 노출, 즐겨찾기 필터 결합, SVG Favicon 확인. |

## v1.2.1. 2026-06-14.

- 즐겨찾기 버튼을 더 작은 오른쪽 하단 버튼으로 옮기고 카드 태그와 겹치지 않게 본문 공간을 확보했다.
- Compose가 앱 시작 전에 호스트 데이터 디렉터리 권한을 자동 준비해 기본 Docker 배포에서도 SQLite 즐겨찾기를 쓸 수 있게 했다.
- 실제 Chromium 클릭과 배치 조건, Compose 데이터 초기화 계약을 회귀 테스트로 추가했다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 자동 테스트 | 로컬과 GitHub Actions에서 68개 통과. |
| 코드 포맷 | `dotnet format Raider.slnx --verify-no-changes --no-restore` 통과. |
| 빌드 및 경고 | `dotnet build Raider.slnx --no-restore -warnaserror` 경고 0개, 오류 0개. |
| 실제 브라우저 | 즐겨찾기 추가·삭제·사이드바 반영, 44px 클릭 영역, 태그 비겹침 확인. |
| 컨테이너 게시 | GitHub Actions에서 `1.2.1`, `1.2`, `latest` GHCR 이미지 게시 성공. |

## v1.2.1 이후 운영 정리.

- GitHub Actions가 Node.js 24 런타임을 사용하는 최신 메이저 액션을 사용하도록 갱신했다.
- Traefik 배포에서도 데이터 초기화 서비스와 즐겨찾기 SQLite 경로 설정을 유지하도록 운영 문서를 보강했다.

## v1.2.0. 2026-06-14.

### 제공 기능.

- **서버 공용 즐겨찾기**: 모든 접속 기기가 하나의 방송인 즐겨찾기 목록을 공유하며 `(Platform, ChannelId)`로 중복 없이 저장한다.
- **라이브 상태 사이드바**: 데스크톱 왼쪽 접이식 사이드바와 모바일 드로어에서 즐겨찾기 방송인의 라이브, 오프라인, 상태 확인 지연을 표시한다.
- **호스트 영속 저장소**: SQLite 파일을 서버 호스트 데이터 디렉터리에서 bind mount해 컨테이너 재생성과 이미지 업데이트 뒤에도 유지한다.
- **안전한 변경 API**: 현재 스냅샷에 존재하는 canonical 채널만 추가하며 쓰기 요청에 antiforgery 검증을 적용한다.

### 핵심 결정.

- 사용자 계정과 로그인 없이 개인 서버 전체가 하나의 공용 즐겨찾기 목록을 사용한다.
- 최신 수집 성공 목록에 있으면 라이브, 없으면 오프라인이며 수집 실패 또는 오래된 상태는 상태 확인 지연으로 구분한다.
- 즐겨찾기 DB 초기화와 접근 실패는 라이브 수집과 기본 홈 화면에 전파하지 않는다.
- SQLite 접근에는 `Microsoft.Data.Sqlite`를 직접 사용하고 Entity Framework Core는 도입하지 않는다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 자동 테스트 | 67개 통과. |
| 코드 포맷 | `dotnet format --verify-no-changes` 통과. |
| 빌드 및 경고 | `dotnet build --no-restore -warnaserror` 경고 0개, 오류 0개. |
| 실제 브라우저 | 데스크톱 사이드바 접힘과 모바일 375px 드로어·스크롤 잠금·가로 스크롤 없음 확인. |
| 컨테이너 | 현재 환경에 Docker Compose 플러그인이 없어 bind mount 재생성 검증 미실행. |

## v1.1.1. 2026-06-14.

- 웹 통합 테스트를 실제 CHZZK 인증정보와 백그라운드 수집에서 격리했다.
- GitHub Actions의 깨끗한 환경에서도 합성 인증정보만으로 회귀 테스트를 실행한다.
- GHCR 게시 전에 Playwright Chromium을 설치하고 전체 검증을 실행한다.

## v1.1.0. 2026-06-13.

### 제공 기능.

- **즉시 새로고침**: 마지막 수집 시간 영역에 새로고침 버튼을 제공하며, 비동기 백그라운드 수집 실행 및 실시간 진행 상태 조회(`/api/refresh/status`) API를 통해 수집 완료 시 화면 자동 갱신을 지원한다.
- **2단 컨트롤 뷰**: 플랫폼 필터와 검색창을 상단에, 추천 태그 목록과 드로어를 하단에 분리 구성하여 레이아웃을 간소화하고 가독성을 개선한다.
- **필터 해제/토글**: 이미 활성화된 태그 버튼 재클릭 시 필터가 해제되며, 활성화된 필터 조건(플랫폼, 태그, 검색어)을 개별적으로 닫을 수 있는 활성 필터 칩 영역을 제공한다.
- **인기 태그 드로어**: 기본 8개 추천 태그 외에 최대 30개의 모든 인기 태그를 펼쳐볼 수 있는 아코디언 드로어를 지원한다.
- **방송 카드 내 태그 링크**: 방송 타일의 태그를 클릭 시 해당 태그 필터가 즉시 적용된다.
- **Legibility 및 화면 여백 최적화**: 카드 내 태그들을 투명 배경의 브랜드 색상 텍스트 링크(샵 `#` 기호 제외)로 리스타일링하여 시각적 복잡도를 제거하고, 히어로 영역 및 통계 수치를 슬림화하여 방송 콘텐츠를 더 상단에 노출한다.

### 핵심 결정.

- **비동기 갱신 구조**: 수집 동기 대기로 인한 스레드 블로킹을 배제하고, 백그라운드 스레드에서 수집이 돌도록 유도하며 UI에서 가벼운 JS 폴링으로 갱신을 감지한다.
- **순수 HTML/CSS/Vanilla JS**: 프레임워크나 중대형 외부 라이브러리 없이 2단 토글 및 드로어, 폴링 로직을 구축한다.
- **# 접두사 배제**: 해시태그 디자인을 도입하되 불필요한 '#' 기호를 완전히 제거하고 순수 텍스트 브랜드 색상만으로 단순성을 강화한다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 자동 테스트 | 60개 통과. |
| 코드 포맷 | dotnet format 검사 규칙 완벽 준수. |
| 빌드 및 경고 | 에러 0개, 경고 0개 빌드 성공. |
| 반응형 UI | 데스크톱 2단 구성 및 모바일 수직 스택 반응형 검증 완료. |

## v1.0.0. 2026-06-13.

### 제공 기능.

- CHZZK 공식 Open API와 SOOP 공개 웹 JSON에서 현재 라이브 전체 목록을 독립적으로 수집한다.
- 플랫폼 장애를 격리하고 마지막 정상 목록을 유지하는 불변 메모리 스냅샷을 제공한다.
- 플랫폼, 방송인 이름, 제목, 태그 검색과 필터, 시청자 수 정렬, 서버 측 페이지네이션을 제공한다.
- 반응형 Razor Pages 타일 화면과 수집 상태 화면을 제공한다.
- 비루트, 읽기 전용 루트, 무볼륨 단일 컨테이너 배포를 지원한다.

### 핵심 결정.

- .NET 10과 ASP.NET Core 모듈식 모놀리스를 사용한다.
- 화면 요청은 외부 호출이나 잠금 없이 현재 스냅샷만 읽는다.
- CHZZK은 공식 API를 사용하고 SOOP은 공식 Client ID를 확보할 때까지 공개 웹 JSON을 사용한다.
- 영구 데이터베이스, Redis, 외부 검색 엔진, 프론트엔드 프레임워크를 도입하지 않는다.
- 코드 품질은 적은 개념, 명확한 이름, 직접적인 데이터 흐름을 우선한다.

### 외부 수집 계약.

- CHZZK 공식 API는 최대 페이지 크기 20과 `page.next` cursor를 사용하며 완성된 전체 순회만 반영한다.
- SOOP 공개 JSON은 페이지당 60건이며 현재 시청자 수로 `current_view_cnt`를 사용한다. `total_view_cnt`는 실제 비교에서 현재 시청자 수와 달라 제외했다.
- SOOP 순회는 시청자 수 정렬보다 중복이 적었던 `broad_start` 정렬을 사용한다.
- SOOP 요청은 로그인, API 키, 요청 쿠키 없이 수행한다. 응답이 설정하는 지역 판별 쿠키도 저장하거나 재전송하지 않는다.

### 검증 결과.

| 항목 | 결과 |
| --- | --- |
| 자동 테스트 | 59개 통과. |
| 검색 성능 | 방송 5,000건과 태그 100개 조합 검색 p95 `0.216ms`. |
| 홈 화면 | 실제 수집 중 p95 `35.321ms`, 준비 완료 후 p95 `10.034ms`. |
| 반응형 UI | 1440px 데스크톱 4열과 375px 모바일 1열, 가로 스크롤 없음 Playwright 검증 통과. |
| 컨테이너 | Docker Engine `29.5.2`, 비루트, 읽기 전용, 무볼륨, 재생성 복구 검증 통과. |
| 자원 사용 | 실제 전체 수집 후 메모리 약 `73MB`, 상한 CPU 1·메모리 256MB·PID 128. |
| 외부 수집 | CHZZK과 SOOP 전체 페이지 실제 API 스모크 통과. |
| 보안 | 비밀값, 쿠키, 원본 JSON, 사용자 검색어의 로그와 HTML 비노출 확인. |

### 알려진 운영 위험.

- SOOP 공개 웹 JSON은 비공식 계약이므로 예고 없이 변경되거나 차단될 수 있다.
- 두 플랫폼의 동적 페이지네이션은 한 번의 순회에서 중복이나 누락을 만들 수 있다. 중복은 제거하고 누락은 다음 폴링에서 복구한다.
- 제한 초과 응답은 다음 기본 폴링 주기에 다시 시도하며 현재 `Retry-After` 헤더는 반영하지 않는다.
- 실제 외부 API 스모크 테스트는 자격 증명과 네트워크가 필요하므로 반복 가능한 기본 테스트와 분리한다.

### 완료된 구현 단계.

디자인 승인, 엔지니어링 계약, API 수집 가능성 검증, .NET 기반과 도메인, CHZZK 어댑터, SOOP 어댑터, 수집 오케스트레이션, 웹 UI, 컨테이너 출시 준비를 완료했다. 세부 실행 체크리스트와 일회성 조사 기록은 이 릴리스 기록으로 압축했다.
